{% extends "base.twig" %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block header %}
    {{ parent() }}
{% endblock %}

{% block content %}
    <div class="container-fluid">
        <div class="row">

            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Modifier la catégorie: {{ category[0].getCategoryTitle() }} </h4>
                        <div class="basic-form">
                            <form>
                                <div class="form-row">
                                    <div class="form-group col-md-12">
                                        <label>Titre:</label>
                                        <input type="text" class="form-control" placeholder=" {{ category[0].getCategoryTitle() }}" name="category_title">
                                    </div>
                                </div>
                                <div class="form-row">
                                    <div class="form-group col-md-auto">
                                        <a href="{{ constant('PANEL') }}category" class="btn mb-1 btn-primary btn-md">Retour</a>
                                    </div>
                                    <div class="form-group col-md-auto">
                                        <button type="submit" class="btn mb-1 btn-success btn-md">Enregistrer</button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{ parent() }}
{% endblock %}

{% block footer %}
    {{ parent() }}
{% endblock %}

{% block scripts %}
    {{ parent() }}
{% endblock %}

